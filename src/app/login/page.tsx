import LoginForm from "@/components/auth/LoginForm";

export default function LoginPage() {
  return (
    <div className="container mx-auto h-[calc(100vh-56px)] flex items-center justify-center">
      <LoginForm />
    </div>
  );
}
